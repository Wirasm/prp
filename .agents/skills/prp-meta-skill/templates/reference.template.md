# <Reference / Template Title>

<!--
Skeleton for an extracted reference or output-format file inside a skill.

- references/<x>.md  → detail the agent READS on demand (schemas, patterns, edge cases, long examples).
- templates/<x>.md   → an output shape the result FOLLOWS. For always-needed formats, the parent
                       SKILL.md must say "Before producing output, read this and follow it exactly."

Rules:
- Content moved here during a refactor is VERBATIM — do not change wording that affects behavior/output.
- This file holds ONE topic. Keep it focused. No duplication with SKILL.md.
- Linked one level deep, directly from the parent SKILL.md Resources section.
-->

<Body: the schema / pattern catalog / worked example / output skeleton goes here.>
